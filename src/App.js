// App.js
import React from "react";
import AppContainer from "./containers/AppContainer";

// App component to render AppContainer
const App = () => {
    return (
        <div>
            {/* AppContainer component */}
            <AppContainer />
        </div>
    );
};

export default App;
